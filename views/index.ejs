<!DOCTYPE html>
<html>
  <head>
    <title>Inicio</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body> 
    <main>
      <h2>¡Bienvenido! Registrate para ingresar al sitio</h2>
         
      <% if (locals.user) { %>
        <style>
          body{
            background-color: <%= locals.user.color%>;
          }
          h2{
            display: none;
          }
        </style>
        <h1>¡Hola <%= locals.user.name %>!</h1>
        <p>Elegiste el color <%= locals.user.color %> </p>
        <p>Tu email es: <%= locals.user.email %> </p>
        <p>Y tu edad es: <%= locals.user.edad =! "" ? locals.user.edad : "No lo se" %> </p>
        <a href="/siguiente">Siguiente</a>
        
     <% } else { %> 
      <form action="" method="POST">
        <label for="name">Ingrese su nombre</label>
        <input type="text" id="name" name="name">
        <span><%= locals.errors && errors.name ? errors.name.msg : null %> </span>
        <div>
          <select name="color" id="color">
              <option value="" hidden>Elija un color</option>
              <option value="red">Rojo</option>
              <option value="blue">Azul</option>
              <option value="green">Verde</option>
              <option value="yellow">Amarillo</option>
          </select>
          <span><%= locals.errors && errors.colors ? errors.colors.msg : null %></span>
        </div>
        <label for="email">Ingrese su email</label>
        <input type="email" name="email" id="email">
        <span><%= locals.errors && errors.email ? errors.email.msg : null %> </span>
        <label for="edad">Ingrese su edad</label>
        <input type="number" name="edad" id="edad">
        <label for="recordar">Recordar color</label>
        <input type="checkbox" name="recordar" id="recordar">
        <button type="submit">Enviar</button>
      </form>
      <% } %>
    </main>
  </body>
</html>
